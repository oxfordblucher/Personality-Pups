<h1>Burgers!</h1>

<h2>To Be Devoured!</h2>
<ol>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burgers-to-eat}}
    {{/unless}}
  {{/each}}
</ol>

<h2>Devoured List!</h2>
<ol>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block }}
    {{/if}}
  {{/each}}
</ol>

<h2>Full List</h2>
<ol>
    {{#each burgers}}
        {{> burgers/burger-block}}
    {{/each}}
</ol>

<h2>Cook up a burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bg">Burger name/fixin's:</label>
    <input type="text" id="bg" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>
